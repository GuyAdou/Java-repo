char c = 'k'; // example character

if (c >= 'b' && c <= 'g') { // check if the character is between 'b' and 'g'
  System.out.println("The character is between 'b' and 'g'.");
} else {
  System.out.println("The character is not between 'b' and 'g'.");
}
